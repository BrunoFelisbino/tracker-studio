# Copy to .env for local development. Never commit real values.

# Optional integration URL. Credentials are configured in the app and stored
# using the operating system credential manager.
TRACKER_STUDIO_API_BASE_URL=
